/**
 * @typedef {object} FixedLoopLengthConditionConfig
 * @property {object} expression regular expression that will match `.length` property. For instance, for JavaScript it could be: `/(\w+).length/` while for C# `/(\w+).Length/`
 */
