/**
 * @typedef {object} Prefix
 * @property {string} value readable name
 * @property {string} meaning
 */

/**
 * @typedef {object} MarkedHTMLCommentConfig
 * @property {Array<Prefix>} prefixes array of prefixes that should be used in comments
 */
