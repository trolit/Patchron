const js = require('./js');
const vue = require('./vue');
const html = require('./html');
const pull = require('./pull');
const common = require('./common');

module.exports = {
    js,
    vue,
    html,
    pull,
    common
};
