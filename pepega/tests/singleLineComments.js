// my new function
module.exports = () => {
    // x - const number
    const x = 2;

    // TODO: add more comments
    if (x) {
        const result = x * 5;

        // TMP:
        console.log(result);
    }

    // !: this is deprecated!
    console.log('egg');

    // ?: should I add more comments here?
};
