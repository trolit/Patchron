/**
 * This is my first javascript file
 * check the docs to see what does that function do
 */
module.exports = () => {
    /* here I have x = 2 */
    const x = 2;

    /**
     * TODO: Fix that part
     */
    if (x) {
        const result = x * 5;

        // TMP:
        console.log(result);
    }

    // !: this is deprecated!
    console.log('egg');

    // ?: should I add more comments here?
};
